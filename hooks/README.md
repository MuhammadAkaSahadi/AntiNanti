# Hooks Directory

This directory contains custom React hooks for the AntiNanti project.
Use this directory for sharing stateful or effectful logic (e.g., auth hooks, custom fetching, event listeners).
