# Components Directory

This directory contains reusable React components for the AntiNanti project.
Follow the rules in `GEMINI.md`:
- Use Server Components by default.
- Use `'use client'` only on low-level interactive elements (forms, chat buttons, modal dialogs).
- Use Tailwind CSS and ShadcnUI.
